[project]
name = "payroll"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "jinja2>=3.1",
    "uvicorn[standard]>=0.32",
    "pyjwt[crypto]>=2.9",
    # <49: later versions publish no Intel-Mac wheel (source build needs OpenSSL + pkg-config).
    "cryptography<49",
    # zoneinfo needs a time zone database; this guarantees one regardless of the runtime's own.
    "tzdata>=2024.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    # For fastapi.testclient.TestClient only — not used at runtime.
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
