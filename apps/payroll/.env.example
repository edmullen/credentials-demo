# Copy to .env and run `uv run tools/generate_credentials.py` from the repo root, which writes
# apps/payroll/.env itself (docs/design.md §3). This file only documents the variable's shape.
PAYROLL_SIGNING_KEY={"kty":"EC","crv":"P-256","x":"...","y":"...","d":"...","kid":"payroll-1"}
