# Copy to .env and run `uv run tools/generate_credentials.py` from the repo root, which writes
# apps/benefits/.env itself (docs/design.md §3). This file only documents the variable's shape.
BENEFITS_SIGNING_KEY={"kty":"EC","crv":"P-256","x":"...","y":"...","d":"...","kid":"benefits-1"}
